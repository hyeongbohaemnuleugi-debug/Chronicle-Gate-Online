{
  "name": "chronicle-gate-online",
  "version": "10.0.0",
  "private": true,
  "type": "module",
  "description": "Narrative-first 1-4 player online TRPG with scene-linked choice-only storytelling, job-specific openings, fail-forward checks, contextual item routes, authored ending dilemmas, persistent accounts, and nine chronicles",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node scripts/qa.mjs",
    "test": "node --test tests-release/*.test.mjs",
    "verify": "npm run check && npm test",
    "test:legacy": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "express": "5.2.1",
    "socket.io": "4.8.3",
    "three": "0.185.1"
  },
  "engines": {
    "node": ">=20 <23"
  }
}
